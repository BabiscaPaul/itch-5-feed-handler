import { Center, Text } from '@mantine/core';

export function OverviewPage() {
  return (
    <Center h={400}>
      <Text c="dimmed" ff="monospace" size="sm" style={{ letterSpacing: '0.12em', textTransform: 'uppercase' }}>
        Overview — blank for now
      </Text>
    </Center>
  );
}
